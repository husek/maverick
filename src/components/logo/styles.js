import styled from 'styled-components';

export const WaterMark = styled.img`
  width: 300px;
  opacity: 0.25;
  float: right;
  margin-top: 0.8em;
  margin-right: -1.5em;
`;

export const LogoWrapper = styled.div`
  h2 {
    color: #a0c3ad;
    letter-spacing: 0.25em;
    font-weight: bold;
    text-transform: uppercase;
    text-shadow: -20px 19px 0 #002f12;
  }
`
